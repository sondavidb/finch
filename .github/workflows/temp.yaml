name: Temp

on:
  push:
    branches: ['main', 'release/**']
    paths:
      - 'deps/finch-core'

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: hello
        run: |
          echo "hello"
